
QsMessengerServer 1.0.2a Preview Available  

Main changes:

- Relay functions have all been adapted for instant forwarding;
- Almost all database queries use "email" as primary key; 
- ( The database also has some changes; )

* sessionId still to be removed;
  
It is still a new preview, other errors are not excluded, but it does seem to be ok considering the amount of changed parameters.  

  
Other planned changes:  
  
- Server Ip Filter by region;
  
